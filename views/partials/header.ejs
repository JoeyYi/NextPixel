<!DOCTYPE html>
<html>
    <head>
        <title>Next Pixel</title>
        <meta name="description" content="Share your favorite scenes in 2 dimension world." />
        <link rel="stylesheet" type="text/css" href="/css/main.css">
        <link rel="stylesheet" type="text/css" href="/css/skeleton.css">
    </head>
    <body>
        <div class="navbar">
            <div class="navbar-list">

                <a class="navbar-item" href="/">NEXT PIXEL</a>
                <div class="right">
                    <% if(!currentUser){ %>
                        <a class="navbar-item" href="/login">Login</a>
                        <a class="navbar-item" href="/register">Sign Up</a>
                    <% } else { %>
                        <a class="navbar-item" href="#"><%= currentUser.username %></a>
                        <a class="navbar-item" href="/logout">Logout</a>
                    <% } %>
                </div>


            </div>
        </div>
        <% if(flash && flash.length > 0){ %>
            <p id="flash"><%= flash %></p>
        <% } %>
 
